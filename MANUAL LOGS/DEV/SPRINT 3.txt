Worked for 42s
Worked for 42s
Worked for 42s
Worked for 42s
Worked for 42s
Worked for 42s

Yes, Director. I have now treated this as an architecture problem, not a “make the map look right” problem.

I re-read the current repository, the roadmap, the Sprint 2 log, the actual current map scripts, scene configuration, and the GIS source notes, and I also checked the relevant Godot 4.7 APIs for transforms, input, and polygon hit-testing. The repository currently has a very small rendering stack: india_map.gd loads 543 seats, calculates bounds, projects them into a fixed 1200×1000 rectangle, and draws only polylines. There is no persistent map transform, no polygon cache, no hit-testing, no zoom/pan controller, and no selection UI. 
G
GitHub
+3

The roadmap explicitly says the GIS gate should render the map, allow individual constituency selection, and display constituency identity; then Gate 3 builds the tiny political prototype. 
G
GitHub

So I'm going to make one important decision:

Project 543 Map Architecture — LOCKED

We will not keep tuning MAP_WIDTH and MAP_HEIGHT.

The map will have:

GIS DATA
   ↓
GEOMETRY CACHE
   ↓
CANONICAL MAP COORDINATES
   ↓
FIT-TO-VIEWPORT
   ↓
UNIFORM ZOOM
   ↓
PAN
   ↓
SCREEN


And input goes the opposite direction:

SCREEN CLICK
   ↓
inverse map transform
   ↓
CANONICAL MAP COORDINATE
   ↓
candidate constituency lookup
   ↓
point-in-polygon
   ↓
543-seat identity


Godot's Node2D/Transform2D system is designed for exactly this sort of translation/scale architecture, and Geometry2D.is_point_in_polygon() provides the polygon selection primitive we need. 
G
Godot Engine documentation
+2

This means zooming will never alter GIS data.

First: one important GIS warning

The current 543-seat GeoJSON came from the 2024 Maps Supplement. Its own author explicitly says it is intended for visualization and is not research-quality geometry, and notes that the all-India boundaries can be off because they were georeferenced from images. 
G
GitHub

Your Sprint 2 log already correctly recognized this distinction. 
G
GitHub

So:

We can make the map engine production-quality without claiming that the underlying prototype GIS geometry is legally authoritative.

Later, we can swap the dataset without rewriting the map engine.

That is exactly the architecture we want.

What we're building now

After this change:

Complete India map automatically fits the window.
No manual MAP_WIDTH/MAP_HEIGHT tuning.
Uniform scaling prevents stretching.
Window resizing automatically refits.
Mouse-wheel zoom.
Zoom toward cursor.
Middle-mouse pan.
Left-click constituency selection.
Hover highlighting.
Selected constituency highlighting.
543 individual identities preserved.
MultiPolygon support.
Polygon hit-testing.
Fast bounding-box rejection before polygon testing.
Double-click resets map.
Home resets map.
+ / - zoom.
Map cannot be permanently dragged away.
Minimum zoom always shows the complete map.
Maximum zoom is bounded.
Selection remains correct at every zoom.
Map geometry is cached rather than repeatedly parsed.
Constituency information can be emitted cleanly to the future UI.
F5 will launch the map rather than the obsolete Sprint-1 label.
The old duplicate scene wrapper will be removed.

That is the foundation we should not have to revisit later.

FILE 1 — scripts/geo_geometry.gd

Replace the current file with:

class_name GeoGeometry
extends RefCounted


static func extract_points(geometry: Dictionary) -> Array:
	var geometry_type: String = str(
		geometry.get("type", "")
	)

	var coordinates: Variant = geometry.get(
		"coordinates",
		[]
	)

	var rings: Array = []

	if geometry_type == "Polygon":
		for ring: Variant in coordinates:
			rings.append(ring)

	elif geometry_type == "MultiPolygon":
		for polygon: Variant in coordinates:
			for ring: Variant in polygon:
				rings.append(ring)

	return rings


static func extract_polygons(geometry: Dictionary) -> Array:
	var geometry_type: String = str(
		geometry.get("type", "")
	)

	var coordinates: Variant = geometry.get(
		"coordinates",
		[]
	)

	var polygons: Array = []

	if geometry_type == "Polygon":
		var polygon_rings: Array = []

		for ring: Variant in coordinates:
			polygon_rings.append(ring)

		if not polygon_rings.is_empty():
			polygons.append(polygon_rings)

	elif geometry_type == "MultiPolygon":
		for polygon: Variant in coordinates:
			var polygon_rings: Array = []

			for ring: Variant in polygon:
				polygon_rings.append(ring)

			if not polygon_rings.is_empty():
				polygons.append(polygon_rings)

	return polygons

Why this changes

The existing implementation flattens:

MultiPolygon
    ↓
all rings


and loses the relationship between:

outer boundary
      +
holes


For rendering lines that's acceptable.

For professional selection logic, it isn't.

Now we preserve:

Seat
 ├── Polygon 1
 │    ├── outer ring
 │    └── hole(s)
 │
 └── Polygon 2
      ├── outer ring
      └── hole(s)

FILE 2 — scripts/india_map.gd

This is the main upgrade.

Replace the current file completely with:

extends Node2D


signal constituency_selected(seat: Dictionary)
signal constituency_hovered(seat: Dictionary)
signal constituency_cleared()


const DATA_PATH := "res://data/generated/india_ls_seats_543_runtime.geojson"

# Canonical map width.
# Height is calculated from the actual geographic bounds.
const MAP_WIDTH := 1600.0

# Screen-space behaviour.
const FIT_PADDING := 32.0
const MIN_ZOOM := 1.0
const MAX_ZOOM := 12.0
const ZOOM_FACTOR := 1.15

# Interaction.
const PAN_BUTTON := MOUSE_BUTTON_MIDDLE

# Rendering.
const BASE_LINE_WIDTH := 1.0
const HOVER_LINE_WIDTH := 2.0
const SELECTED_LINE_WIDTH := 3.0

const MAP_LINE_COLOR := Color(0.82, 0.86, 0.92, 0.92)
const MAP_HOVER_COLOR := Color(0.35, 0.78, 1.0, 1.0)
const MAP_SELECTED_COLOR := Color(1.0, 0.78, 0.22, 1.0)

const MAP_BACKGROUND_COLOR := Color(0.035, 0.055, 0.09, 1.0)


var seats: Array = []

var projection: GeoProjection

# Cached projected geometry.
# Each entry:
# {
#     "seat": Dictionary,
#     "polygons": Array[Array[PackedVector2Array]],
#     "bounds": Rect2
# }
var seat_shapes: Array = []


var map_size := Vector2.ZERO

# Fit scale required to show the complete map.
var fit_scale := 1.0

# User zoom multiplier.
var zoom_level := MIN_ZOOM

# Actual screen scale.
var view_scale := 1.0


var selected_seat_index := -1
var hovered_seat_index := -1

var is_panning := false
var last_mouse_position := Vector2.ZERO


func _ready() -> void:
	set_process_input(true)

	var loader := GISDataLoader.new()

	seats = loader.load_seats(DATA_PATH)

	print("GIS seats loaded: ", seats.size())

	assert(
		seats.size() == 543,
		"Project 543 requires exactly 543 constituencies."
	)

	if not _validate_seats():
		return

	if not _build_projection():
		return

	_build_geometry_cache()

	_update_view_transform(true)

	queue_redraw()


func _notification(what: int) -> void:
	if what == NOTIFICATION_WM_SIZE_CHANGED:
		call_deferred("_handle_viewport_resized")


func _handle_viewport_resized() -> void:
	if projection == null:
		return

	_update_view_transform(true)
	queue_redraw()


func _build_projection() -> bool:
	var bounds := GISBounds.new()

	for seat in seats:
		var geometry: Dictionary = seat["geometry"]

		var polygons := GeoGeometry.extract_polygons(
			geometry
		)

		for polygon in polygons:
			for ring in polygon:
				for coordinate in ring:
					if coordinate.size() < 2:
						continue

					var longitude := float(coordinate[0])
					var latitude := float(coordinate[1])

					bounds.include_coordinate(
						longitude,
						latitude
					)

	if not bounds.is_valid():
		push_error("Could not calculate GIS bounds")
		return false

	print(
		"Bounds: ",
		bounds.min_lon,
		", ",
		bounds.min_lat,
		" -> ",
		bounds.max_lon,
		", ",
		bounds.max_lat
	)

	# Preserve the actual geographic aspect ratio.
	#
	# We do NOT force the map into an arbitrary 1200x1000 rectangle.
	var longitude_range := bounds.max_lon - bounds.min_lon
	var latitude_range := bounds.max_lat - bounds.min_lat

	if longitude_range <= 0.0 or latitude_range <= 0.0:
		push_error("Invalid GIS bounds range")
		return false

	var map_height := (
		MAP_WIDTH
		* latitude_range
		/ longitude_range
	)

	map_size = Vector2(
		MAP_WIDTH,
		map_height
	)

	projection = GeoProjection.new()

	projection.setup(
		bounds.min_lon,
		bounds.max_lon,
		bounds.min_lat,
		bounds.max_lat,
		map_size.x,
		map_size.y
	)

	print(
		"Canonical map size: ",
		map_size
	)

	return true


func _build_geometry_cache() -> void:
	seat_shapes.clear()

	for seat in seats:
		var projected_polygons: Array = []
		var combined_bounds := Rect2()

		var first_bounds := true

		var geometry: Dictionary = seat["geometry"]

		var polygons := GeoGeometry.extract_polygons(
			geometry
		)

		for polygon in polygons:
			var projected_rings: Array = []

			for ring in polygon:
				var points := PackedVector2Array()

				for coordinate in ring:
					if coordinate.size() < 2:
						continue

					var longitude := float(
						coordinate[0]
					)

					var latitude := float(
						coordinate[1]
					)

					points.append(
						projection.project(
							longitude,
							latitude
						)
					)

				if points.size() >= 3:
					projected_rings.append(points)

					var ring_rect := _get_points_bounds(
						points
					)

					if first_bounds:
						combined_bounds = ring_rect
						first_bounds = false
					else:
						combined_bounds = combined_bounds.merge(
							ring_rect
						)

			if not projected_rings.is_empty():
				projected_polygons.append(
					projected_rings
				)

		seat_shapes.append(
			{
				"seat": seat,
				"polygons": projected_polygons,
				"bounds": combined_bounds
			}
		)

	print(
		"Geometry cache built: ",
		seat_shapes.size(),
		" constituencies"
	)


func _get_points_bounds(
	points: PackedVector2Array
) -> Rect2:
	if points.is_empty():
		return Rect2()

	var min_point := points[0]
	var max_point := points[0]

	for point in points:
		min_point.x = min(min_point.x, point.x)
		min_point.y = min(min_point.y, point.y)

		max_point.x = max(max_point.x, point.x)
		max_point.y = max(max_point.y, point.y)

	return Rect2(
		min_point,
		max_point - min_point
	)


func _update_view_transform(
	reset_zoom: bool = false
) -> void:
	var viewport_size := get_viewport_rect().size

	if viewport_size.x <= 0.0 or viewport_size.y <= 0.0:
		return

	var available_size := viewport_size - Vector2.ONE * (
		FIT_PADDING * 2.0
	)

	if available_size.x <= 0.0 or available_size.y <= 0.0:
		return

	fit_scale = min(
		available_size.x / map_size.x,
		available_size.y / map_size.y
	)

	if fit_scale <= 0.0:
		fit_scale = 1.0

	if reset_zoom:
		zoom_level = MIN_ZOOM

	_apply_zoom_and_center()


func _apply_zoom_and_center() -> void:
	var viewport_size := get_viewport_rect().size

	view_scale = fit_scale * zoom_level

	scale = Vector2.ONE * view_scale

	position = (
		viewport_size * 0.5
		- map_size * view_scale * 0.5
	)

	_clamp_map_position()


func _clamp_map_position() -> void:
	var viewport_size := get_viewport_rect().size
	var rendered_size := map_size * view_scale

	var min_position := Vector2.ZERO
	var max_position := Vector2.ZERO

	if rendered_size.x <= viewport_size.x:
		var centered_x := (
			viewport_size.x
			- rendered_size.x
		) * 0.5

		min_position.x = centered_x
		max_position.x = centered_x
	else:
		min_position.x = (
			viewport_size.x
			- rendered_size.x
		)
		max_position.x = 0.0

	if rendered_size.y <= viewport_size.y:
		var centered_y := (
			viewport_size.y
			- rendered_size.y
		) * 0.5

		min_position.y = centered_y
		max_position.y = centered_y
	else:
		min_position.y = (
			viewport_size.y
			- rendered_size.y
		)
		max_position.y = 0.0

	position.x = clampf(
		position.x,
		min_position.x,
		max_position.x
	)

	position.y = clampf(
		position.y,
		min_position.y,
		max_position.y
	)


func _input(event: InputEvent) -> void:
	if projection == null:
		return

	if event is InputEventMouseButton:
		_handle_mouse_button(event)
		return

	if event is InputEventMouseMotion:
		_handle_mouse_motion(event)
		return

	if event is InputEventKey:
		_handle_key(event)


func _handle_mouse_button(
	event: InputEventMouseButton
) -> void:
	if event.button_index == MOUSE_BUTTON_WHEEL_UP:
		if event.pressed:
			_zoom_at(
				event.position,
				ZOOM_FACTOR
			)
		return

	if event.button_index == MOUSE_BUTTON_WHEEL_DOWN:
		if event.pressed:
			_zoom_at(
				event.position,
				1.0 / ZOOM_FACTOR
			)
		return

	if event.button_index == PAN_BUTTON:
		is_panning = event.pressed

		if event.pressed:
			last_mouse_position = event.position

		return

	if event.button_index == MOUSE_BUTTON_LEFT:
		if event.pressed:
			_select_at_screen_position(
				event.position
			)

		return

	if (
		event.button_index == MOUSE_BUTTON_LEFT
		and event.double_click
	):
		_reset_view()


func _handle_mouse_motion(
	event: InputEventMouseMotion
) -> void:
	var screen_position := event.position

	if is_panning:
		var delta := (
			screen_position
			- last_mouse_position
		)

		position += delta

		_clamp_map_position()

		last_mouse_position = screen_position

		queue_redraw()

		return

	_update_hover(
		screen_position
	)


func _handle_key(event: InputEventKey) -> void:
	if not event.pressed or event.echo:
		return

	match event.keycode:
		KEY_HOME:
			_reset_view()

		KEY_EQUAL:
			_zoom_at(
				get_viewport_rect().size * 0.5,
				ZOOM_FACTOR
			)

		KEY_PLUS:
			_zoom_at(
				get_viewport_rect().size * 0.5,
				ZOOM_FACTOR
			)

		KEY_MINUS:
			_zoom_at(
				get_viewport_rect().size * 0.5,
				1.0 / ZOOM_FACTOR
			)


func _zoom_at(
	screen_position: Vector2,
	factor: float
) -> void:
	var old_scale := view_scale

	var new_zoom := clampf(
		zoom_level * factor,
		MIN_ZOOM,
		MAX_ZOOM
	)

	if is_equal_approx(
		new_zoom,
		zoom_level
	):
		return

	# Convert the cursor from screen space
	# into canonical map space BEFORE changing scale.
	var map_point := (
		screen_position - position
	) / old_scale

	zoom_level = new_zoom
	view_scale = fit_scale * zoom_level

	scale = Vector2.ONE * view_scale

	# Keep the geographic point underneath
	# the cursor stationary while zooming.
	position = (
		screen_position
		- map_point * view_scale
	)

	_clamp_map_position()

	queue_redraw()


func _reset_view() -> void:
	zoom_level = MIN_ZOOM
	_apply_zoom_and_center()

	queue_redraw()


func _select_at_screen_position(
	screen_position: Vector2
) -> void:
	var seat_index := _find_seat_at_screen_position(
		screen_position
	)

	if seat_index == -1:
		selected_seat_index = -1

		constituency_cleared.emit()

		queue_redraw()

		return

	selected_seat_index = seat_index

	var seat: Dictionary = seat_shapes[
		seat_index
	]["seat"]

	constituency_selected.emit(
		seat
	)

	queue_redraw()


func _update_hover(
	screen_position: Vector2
) -> void:
	var seat_index := _find_seat_at_screen_position(
		screen_position
	)

	if seat_index == hovered_seat_index:
		return

	hovered_seat_index = seat_index

	if hovered_seat_index == -1:
		queue_redraw()
		return

	var seat: Dictionary = seat_shapes[
		hovered_seat_index
	]["seat"]

	constituency_hovered.emit(
		seat
	)

	queue_redraw()


func _find_seat_at_screen_position(
	screen_position: Vector2
) -> int:
	if seat_shapes.is_empty():
		return -1

	# Convert screen coordinates into map-local
	# coordinates using the inverse of our
	# current uniform transform.
	var map_position := (
		screen_position - position
	) / view_scale

	# Test in reverse draw order so later shapes
	# have deterministic priority when boundaries
	# touch.
	for index in range(
		seat_shapes.size() - 1,
		-1,
		-1
	):
		var entry: Dictionary = seat_shapes[index]

		var bounds: Rect2 = entry["bounds"]

		if not bounds.has_point(map_position):
			continue

		var polygons: Array = entry["polygons"]

		for polygon in polygons:
			if polygon.is_empty():
				continue

			var outer_ring: PackedVector2Array = (
				polygon[0]
			)

			if not Geometry2D.is_point_in_polygon(
				map_position,
				outer_ring
			):
				continue

			# GeoJSON Polygon rings after the first
			# are holes. If the point is inside a hole,
			# it does not belong to this polygon.
			var inside_hole := false

			for hole_index in range(
				1,
				polygon.size()
			):
				var hole_ring: PackedVector2Array = (
					polygon[hole_index]
				)

				if Geometry2D.is_point_in_polygon(
					map_position,
					hole_ring
				):
					inside_hole = true
					break

			if not inside_hole:
				return index

	return -1


func _draw() -> void:
	# Background.
	var viewport_size := get_viewport_rect().size

	draw_rect(
		Rect2(
			Vector2.ZERO,
			viewport_size
		),
		MAP_BACKGROUND_COLOR
	)

	if projection == null:
		return

	for index in seat_shapes.size():
		var entry: Dictionary = seat_shapes[index]

		var polygons: Array = entry["polygons"]

		var line_color := MAP_LINE_COLOR
		var line_width := BASE_LINE_WIDTH

		if index == hovered_seat_index:
			line_color = MAP_HOVER_COLOR
			line_width = HOVER_LINE_WIDTH

		if index == selected_seat_index:
			line_color = MAP_SELECTED_COLOR
			line_width = SELECTED_LINE_WIDTH

		for polygon in polygons:
			for ring in polygon:
				var points: PackedVector2Array = ring

				if points.size() < 2:
					continue

				draw_polyline(
					points,
					line_color,
					line_width / max(view_scale, 0.25),
					true
				)


func _validate_seats() -> bool:
	var ids := {}

	for seat in seats:
		var unique_id: String = str(
			seat["unique_id"]
		)

		if unique_id.is_empty():
			push_error(
				"Seat has empty unique_id"
			)

			return false

		if ids.has(unique_id):
			push_error(
				"Duplicate unique_id: "
				+ unique_id
			)

			return false

		ids[unique_id] = true

	print(
		"Unique seat IDs validated: ",
		ids.size()
	)

	return ids.size() == 543

Important: there is one bug in that code we should correct before you paste it

I deliberately caught it before asking you to run it.

This section:

if event.button_index == MOUSE_BUTTON_LEFT:
	if event.pressed:
		_select_at_screen_position(event.position)

	return

if (
	event.button_index == MOUSE_BUTTON_LEFT
	and event.double_click
):


means the double-click branch can never be reached.

So replace the _handle_mouse_button() function with this version:

func _handle_mouse_button(
	event: InputEventMouseButton
) -> void:
	if event.button_index == MOUSE_BUTTON_WHEEL_UP:
		if event.pressed:
			_zoom_at(
				event.position,
				ZOOM_FACTOR
			)
		return

	if event.button_index == MOUSE_BUTTON_WHEEL_DOWN:
		if event.pressed:
			_zoom_at(
				event.position,
				1.0 / ZOOM_FACTOR
			)
		return

	if event.button_index == PAN_BUTTON:
		is_panning = event.pressed

		if event.pressed:
			last_mouse_position = event.position

		return

	if event.button_index == MOUSE_BUTTON_LEFT:
		if event.double_click and event.pressed:
			_reset_view()
			return

		if event.pressed:
			_select_at_screen_position(
				event.position
			)

		return


That is the version to keep.

But there is one more architectural issue

The current india_map.tscn in GitHub still attaches:

res://Scenes/india_map.gd


rather than the actual implementation. We discovered this during debugging. 
G
GitHub

Your local machine has already been manually fixed, but the repository itself should be corrected.

FILE 3 — Scenes/india_map.tscn

Replace the entire file with:

[gd_scene format=3]

[ext_resource type="Script" path="res://scripts/india_map.gd" id="1_map"]

[node name="IndiaMap" type="Node2D"]
script = ExtResource("1_map")


Now:

IndiaMap
   ↓
scripts/india_map.gd


There is no wrapper.

FILE 4 — DELETE Scenes/india_map.gd

Delete:

project-543/Scenes/india_map.gd


It is redundant.

We don't want:

india_map.tscn
   ↓
Scenes/india_map.gd
   ↓
scripts/india_map.gd


We want:

india_map.tscn
   ↓
scripts/india_map.gd


This removes the exact class of bug that caused your blank map.

FILE 5 — project.godot

We should give the project a deliberate baseline viewport.

Your current project.godot only has:

window/stretch/mode="canvas_items"
window/stretch/aspect="expand"

G
GitHub

Add under [display]:

window/size/viewport_width=1280
window/size/viewport_height=720
window/size/window_width_override=1280
window/size/window_height_override=720
window/stretch/mode="canvas_items"
window/stretch/aspect="expand"


So:

[display]

window/size/viewport_width=1280
window/size/viewport_height=720
window/size/window_width_override=1280
window/size/window_height_override=720
window/stretch/mode="canvas_items"
window/stretch/aspect="expand"


The important point is that the map now adapts to this viewport. If the player runs a different resolution, the map doesn't break.

FILE 6 — Scenes/main.tscn

Currently F5 launches an obsolete Control scene containing:

project543, sprint 1, dev build.

G
GitHub

We should make F5 launch the actual map.

Replace it with:

[gd_scene load_steps=2 format=3]

[ext_resource type="PackedScene" path="res://Scenes/india_map.tscn" id="1_map"]

[node name="Main" type="Node2D"]

[node name="IndiaMap" parent="." instance=ExtResource("1_map")]


Now:

F5
 ↓
Main
 ↓
IndiaMap
 ↓
543 constituencies


and:

F6
 ↓
IndiaMap


both test the same map implementation.

But Director — we're not done yet

The above creates the map engine.

We also need the player to actually know what they clicked.

The map should therefore expose:

signal constituency_selected(seat: Dictionary)


which we already included.

That is intentional.

The map doesn't create a political-information UI.

It says:

"The player selected constituency X."

The future UI decides what to display.

That follows the architecture in your roadmap: presentation should not become simulation logic. 
G
GitHub

Add the first constituency information panel

For Sprint 3, I recommend a separate presentation script.

FILE 7 — scripts/constituency_panel.gd

Create:

extends PanelContainer


var title_label: Label
var state_label: Label
var id_label: Label
var status_label: Label


func _ready() -> void:
	custom_minimum_size = Vector2(
		300.0,
		180.0
	)

	var margin := MarginContainer.new()

	margin.add_theme_constant_override(
		"margin_left",
		18
	)

	margin.add_theme_constant_override(
		"margin_right",
		18
	)

	margin.add_theme_constant_override(
		"margin_top",
		14
	)

	margin.add_theme_constant_override(
		"margin_bottom",
		14
	)

	add_child(margin)

	var box := VBoxContainer.new()

	box.add_theme_constant_override(
		"separation",
		8
	)

	margin.add_child(box)

	title_label = Label.new()
	title_label.text = "No constituency selected"
	title_label.add_theme_font_size_override(
		"font_size",
		22
	)
	box.add_child(title_label)

	state_label = Label.new()
	state_label.text = "State / UT: —"
	box.add_child(state_label)

	id_label = Label.new()
	id_label.text = "Seat ID: —"
	box.add_child(id_label)

	status_label = Label.new()
	status_label.text = "Click a constituency"
	box.add_child(status_label)


func show_constituency(
	seat: Dictionary
) -> void:
	var seat_name := str(
		seat.get(
			"ls_seat_name",
			"Unknown constituency"
		)
	)

	var state_name := str(
		seat.get(
			"state_ut_name",
			"Unknown State / UT"
		)
	)

	var unique_id := str(
		seat.get(
			"unique_id",
			"Unknown"
		)
	)

	title_label.text = seat_name
	state_label.text = (
		"State / UT: "
		+ state_name
	)

	id_label.text = (
		"Seat ID: "
		+ unique_id
	)

	status_label.text = (
		"Constituency selected"
	)


func clear_constituency() -> void:
	title_label.text = (
		"No constituency selected"
	)

	state_label.text = (
		"State / UT: —"
	)

	id_label.text = (
		"Seat ID: —"
	)

	status_label.text = (
		"Click a constituency"
	)

FILE 8 — New map scene with panel

Now we need the map and panel to coexist.

Replace Scenes/india_map.tscn with:

[gd_scene load_steps=3 format=3]

[ext_resource type="Script" path="res://scripts/india_map.gd" id="1_map"]
[ext_resource type="Script" path="res://scripts/constituency_panel.gd" id="2_panel"]

[node name="IndiaMap" type="Node2D"]
script = ExtResource("1_map")

[node name="HUD" type="CanvasLayer" parent="."]

[node name="Panel" type="PanelContainer" parent="HUD"]
offset_left = 24.0
offset_top = 24.0
offset_right = 324.0
offset_bottom = 204.0
mouse_filter = 2
script = ExtResource("2_panel")


Now the architecture is:

IndiaMap
│
├── map renderer
│
└── HUD
    └── ConstituencyPanel


The panel doesn't interfere with map input because:

mouse_filter = 2


means it ignores mouse input.

Connect the panel

Add this to india_map.gd in _ready() after the map has initialized:

	var panel := get_node_or_null(
		"HUD/Panel"
	)

	if panel != null:
		constituency_selected.connect(
			panel.show_constituency
		)

		constituency_cleared.connect(
			panel.clear_constituency
		)


So _ready() ends like:

	_build_geometry_cache()

	_update_view_transform(true)

	var panel := get_node_or_null(
		"HUD/Panel"
	)

	if panel != null:
		constituency_selected.connect(
			panel.show_constituency
		)

		constituency_cleared.connect(
			panel.clear_constituency
		)

	queue_redraw()


This is a clean dependency:

Map
 ↓ signal
Panel


rather than:

Map
 ↓ directly manipulates
UI internals

One correction to the renderer

There is a subtle issue in my first complete code block that I want you to fix before running.

The background is currently being drawn by IndiaMap, which itself is transformed when we zoom.

That means the background would zoom with the map.

We don't want that.

So remove this from _draw():

	var viewport_size := get_viewport_rect().size

	draw_rect(
		Rect2(
			Vector2.ZERO,
			viewport_size
		),
		MAP_BACKGROUND_COLOR
	)


The map should draw only the map.

Instead, add a ColorRect to the HUD.

Change the scene to:

[gd_scene load_steps=3 format=3]

[ext_resource type="Script" path="res://scripts/india_map.gd" id="1_map"]
[ext_resource type="Script" path="res://scripts/constituency_panel.gd" id="2_panel"]

[node name="IndiaMap" type="Node2D"]
script = ExtResource("1_map")

[node name="HUD" type="CanvasLayer" parent="."]

[node name="Background" type="ColorRect" parent="HUD"]
offset_right = 1280.0
offset_bottom = 720.0
color = Color(0.035, 0.055, 0.09, 1)
mouse_filter = 2

[node name="Panel" type="PanelContainer" parent="HUD"]
offset_left = 24.0
offset_top = 24.0
offset_right = 324.0
offset_bottom = 204.0
mouse_filter = 2
script = ExtResource("2_panel")


But there's a layer-order issue: Background must be behind the map, while CanvasLayer is above the Node2D.

So don't use that approach either.

The cleanest solution is to have a separate background ColorRect in main.tscn, behind the map instance.

Therefore, let's simplify.

FINAL scene architecture
Scenes/main.tscn

Use:

[gd_scene load_steps=3 format=3]

[ext_resource type="PackedScene" path="res://Scenes/india_map.tscn" id="1_map"]
[ext_resource type="Script" path="res://scripts/constituency_panel.gd" id="2_panel"]

[node name="Main" type="Node2D"]

[node name="Background" type="ColorRect" parent="."]
offset_right = 1280.0
offset_bottom = 720.0
color = Color(0.035, 0.055, 0.09, 1)
mouse_filter = 2
show_behind_parent = true

[node name="IndiaMap" parent="." instance=ExtResource("1_map")]

[node name="HUD" type="CanvasLayer" parent="."]

[node name="Panel" type="PanelContainer" parent="HUD"]
offset_left = 24.0
offset_top = 24.0
offset_right = 324.0
offset_bottom = 204.0
mouse_filter = 2
script = ExtResource("2_panel")


Then india_map.tscn goes back to being purely the map:

[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/india_map.gd" id="1_map"]

[node name="IndiaMap" type="Node2D"]
script = ExtResource("1_map")


And because the panel is now outside the map scene, we change the panel connection architecture.

main.gd

Create:

project-543/scripts/main.gd


with:

extends Node2D


@onready var map: Node2D = $IndiaMap
@onready var constituency_panel: PanelContainer = $HUD/Panel


func _ready() -> void:
	if map.has_signal(
		"constituency_selected"
	):
		map.constituency_selected.connect(
			constituency_panel.show_constituency
		)

	if map.has_signal(
		"constituency_cleared"
	):
		map.constituency_cleared.connect(
			constituency_panel.clear_constituency
	)


Then add the script to Main:

[node name="Main" type="Node2D"]
script = ExtResource("3_main")


and change load_steps to 4.

This is cleaner.

Why I prefer this architecture

Now we have:

Main
│
├── Background
│
├── IndiaMap
│      │
│      ├── GIS loading
│      ├── projection
│      ├── geometry cache
│      ├── zoom
│      ├── pan
│      ├── hit testing
│      └── selection signal
│
└── HUD
       └── ConstituencyPanel


That is a legitimate presentation architecture.

And later:

Main
│
├── Background
│
├── IndiaMap
│
├── HUD
│   ├── ConstituencyPanel
│   ├── TurnPanel
│   ├── MoneyPanel
│   └── ActionPanel
│
└── GameController


We won't have to rebuild the map.

Map controls

The player gets:

LEFT CLICK
Select constituency

MOUSE WHEEL UP
Zoom in

MOUSE WHEEL DOWN
Zoom out

MIDDLE MOUSE + DRAG
Pan

HOME
Reset map

+
Zoom in

-
Zoom out

DOUBLE CLICK
Reset map


This is the interaction model I recommend for the PC version.

Godot's mouse input events expose mouse position and wheel button events, so handling these at the map level is appropriate. 
G
Godot Engine documentation
+1

The most important technical property

Suppose:

Tamil Nadu constituency


has canonical map coordinates:

(720, 1310)


At 1×:

screen = map * scale + position


At 5×:

screen = map * 5*scale + position


The GIS coordinate never changes.

When the user clicks:

screen
 ↓
inverse transform
 ↓
(720, 1310)
 ↓
point-in-polygon
 ↓
Tamil Nadu constituency


Therefore:

Zoom cannot break selection.

That is the key design principle.

Godot explicitly provides inverse canvas/local transforms for this kind of coordinate conversion, and Node2D supports position/scale transforms. 
G
Godot Engine documentation
+2

Why we don't create 543 buttons

Absolutely not.

We would get:

543 Controls
543 signals
543 scene objects
543 layout problems


Instead:

543 cached polygons
        ↓
one map renderer
        ↓
one hit-testing system


Geometry2D.is_point_in_polygon() is specifically designed to answer whether a point is inside a polygon. 
G
Godot Engine documentation

The bounding-box check happens first:

Mouse
 ↓
Rect2 check
 ↓
only possible polygons
 ↓
point-in-polygon


This keeps the prototype fast.

Important improvement: don't change line width with zoom

We deliberately do:

draw_polyline(
	points,
	line_color,
	line_width / max(view_scale, 0.25),
	true
)


So the visible border stays approximately consistent.

Without this:

zoom 1× → thin line
zoom 12× → enormous line


which would look terrible.

Testing protocol

Once you've made the changes, don't immediately start playing.

Run this exact QA sequence.

Test A — Initial load

F5.

Expected:

Complete India map
centered
entire map visible


No manual map dimensions.

Test B — Resize

Resize the game window.

Expected:

map remains centered
complete map remains visible
no clipping
no distortion

Test C — Zoom

Wheel up.

Expected:

map enlarges
zoom happens toward cursor


Wheel down.

Expected:

map shrinks

Test D — Maximum zoom

Hold wheel up.

Expected:

zoom stops


No infinite zoom.

Test E — Pan

Middle-drag.

Expected:

map moves


Try dragging far beyond the edge.

Expected:

map stops at sensible boundary

Test F — Selection

At minimum zoom:

click a large constituency


Expected:

yellow border
panel updates


Then zoom in.

Click a tiny constituency.

Expected:

correct constituency selected

Test G — South India

Zoom into:

Kerala
Tamil Nadu
Andhra Pradesh
Telangana
Karnataka


Click several seats.

This is particularly important because this is where we've been debugging.

Test H — Northeast

Zoom into:

Assam
Arunachal Pradesh
Nagaland
Manipur
Mizoram
Tripura
Meghalaya


Test several seats.

Test I — Islands

Check:

Andaman & Nicobar
Lakshadweep


The dataset has geographic edge cases, so these need explicit QA.

One thing I do NOT want you to do

Do not try to make the entire country fill the window by changing:

MAP_WIDTH
MAP_HEIGHT


again.

Those are now canonical geometry dimensions.

The screen determines:

fit_scale


The user determines:

zoom_level


The user determines:

position


The GIS determines:

geometry


Those responsibilities are separate.

Sprint 3 map architecture after this

We will have:

                 PROJECT 543 MAP
                       │
             ┌─────────┴─────────┐
             │                   │
          DATA                 VIEW
             │                   │
       543 GeoJSON           fit-to-window
             │                   │
       validation             zoom
             │                   │
       geometry cache           pan
             │                   │
       projection             viewport
             │                   │
             └─────────┬─────────┘
                       │
                 HIT TESTING
                       │
                 ┌─────┴─────┐
                 │           │
               hover       click
                 │           │
                 └─────┬─────┘
                       │
                  constituency
                       │
                       ▼
                 Information UI


This is a stable foundation for the entire game.

One thing I am deliberately NOT claiming

I cannot truthfully say:

"I have executed this exact replacement inside your local Godot installation and verified all 543 seats."

I can inspect your public repository and design/provide the implementation, but I cannot directly manipulate your local checkout from this conversation.

So I want you to treat the first run as a QA build, not as a blind copy-paste promise.

And because we are dealing with 543 real geometries, I want the first acceptance test to be systematic.

Sprint 3 Definition of Done — Map

I am upgrading the original Sprint 2 requirement.

P0
 543 GIS records load
 Unique IDs validated
 Polygon/MultiPolygon supported
 Canonical map coordinates
 Full map automatically fits viewport
 No clipping at default resolution
 Uniform scaling
 Window resize support
 Mouse-wheel zoom
 Cursor-centered zoom
 Middle-mouse pan
 Pan boundaries
 Zoom limits
 Hover detection
 Click detection
 MultiPolygon hit-testing
 Hole-aware hit-testing
 Selected-seat visualization
 Constituency information panel
 F5 launches map
 Duplicate scene wrapper removed
QA
 North India
 South India
 West India
 East India
 Northeast
 Andaman & Nicobar
 Lakshadweep
 Dense urban constituencies
 Very small constituencies
 Window resize
 1× selection
 Maximum zoom selection
And the bigger Project 543 decision

Your roadmap says Gate 2 is the GIS technical spike and Gate 3 is the Tiny Political Prototype: one fictional constituency, one player party, money, support, Rally, Turn, visible support change, then Interview, two action slots, and basic economy. 
G
GitHub

So after this map foundation passes QA, we do not add more map complexity.

The map becomes infrastructure.

Then we move:

MAP
 ↓
SELECT CONSTITUENCY
 ↓
GAME STATE
 ↓
PLAYER PARTY
 ↓
MONEY
 ↓
SUPPORT
 ↓
RALLY
 ↓
INTERVIEW
 ↓
TWO ACTION SLOTS
 ↓
TURN
 ↓
ECONOMY


That is the actual road to a playable Project 543.

One final strategic point: your current GIS source is explicitly a visualization-oriented 2024 dataset rather than research-grade boundary data. 
G
GitHub
 We therefore freeze the map engine but keep the data layer replaceable. When we later acquire/validate a stronger authoritative dataset, we replace india_ls_seats_543_runtime.geojson; the zooming, panning, selection, UI, and simulation architecture remain intact.

That is the version of the map I want Project 543 to build around.

Project 543 repository

G
GitHub
+4
G
G
G
Sources


MAP HAS BEEN FREEEZED, NO MORE DEV IN IT ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++