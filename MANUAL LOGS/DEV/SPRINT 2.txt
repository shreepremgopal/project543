Excellent. **Sprint 1 is officially accepted.**

We now move into the first genuinely important technical milestone.

# PROJECT 543 — SPRINT 2

## GIS / Constituency Map Technical Spike

**Sprint goal:**

> **Prove that real Indian parliamentary constituency geometry can be brought into Project 543, rendered in Godot, and individually selected with the mouse.**

This sprint is deliberately a **technical spike**, not production map development.

We are going to prove the hardest part first.

---

# 1. Important correction to our earlier plan

I have researched the currently available constituency datasets rather than blindly using an old DataMeet file.

The Election Commission's 2024 Atlas confirms that India currently has **543 Parliamentary Constituencies**, and that the 2024 election used the 2008 delimitation framework except for Assam and Jammu & Kashmir, where different boundary arrangements applied. 

There are several publicly available GIS datasets, but we should **not use the old DataMeet national PC shapefile as our production source**, because DataMeet itself warns that its parliamentary constituency data contains pre-delimitation boundaries for several regions and other known issues. ([Datameet Projects][1])

For our technical spike, we'll use a **2024-oriented 543-seat GeoJSON dataset** as the geometry prototype. A maintained open-data collection identifies a 543-constituency dataset sourced from ECI/DataMeet and provides multiple GIS formats. ([Yashveeeeeeer][2])

There is also a 2024 supplementary dataset specifically addressing newer Assam and J&K boundaries and providing a 543-feature India map. Its author explicitly notes that it is suitable for visualization but should not be treated as research-grade survey geometry. ([GitHub][3])

### Therefore:

**Sprint 2 dataset = prototype geometry**

**Production dataset = not yet locked.**

This distinction is important.

We will never silently treat a visualization dataset as authoritative political boundary data.

---

# 2. Sprint 2 Architecture

Our pipeline will be:

```text
2024 constituency GIS data
          ↓
      GeoJSON
          ↓
   Python validation
          ↓
 Project 543 map data
          ↓
       Godot
          ↓
    Polygon rendering
          ↓
     Mouse input
          ↓
 Constituency selection
          ↓
   Information panel
```

Eventually this becomes:

```text
Authoritative / verified GIS sources
              ↓
        GIS pipeline
              ↓
      Validated dataset
              ↓
        Game dataset
              ↓
           Godot
```

---

# 3. Sprint 2 Definition of Done

Sprint 2 is complete only when all of these are true:

* [ ] GIS source acquired
* [ ] GeoJSON inspected
* [ ] Python GIS environment created
* [ ] Dataset parsed successfully
* [ ] Feature count validated
* [ ] Constituency identifiers validated
* [ ] Geometry validity checked
* [ ] Prototype data copied into Project 543
* [ ] One constituency rendered
* [ ] Multiple constituencies rendered
* [ ] All 543 constituencies rendered
* [ ] Constituency can be clicked
* [ ] Selected constituency can be identified
* [ ] Basic information panel displays its identity
* [ ] Map remains responsive
* [ ] GIS spike committed to Git

---

# TASK 2.1 — Create a GIS Workspace

First, we need to modify our project structure.

Inside:

```text
C:\Dev\Project543\
```

create:

```text
tools\
    gis\
```

So:

```text
Project543/
├── assets/
├── data/
├── scenes/
├── scripts/
├── tools/
│   └── gis/
├── tests/
└── docs/
```

Inside `tools\gis`, create:

```text
source\
processed\
scripts\
```

Final structure:

```text
tools/
└── gis/
    ├── source/
    ├── processed/
    └── scripts/
```

### Purpose

`source/`

Raw downloaded GIS files.

`processed/`

Files generated by our pipeline.

`scripts/`

Python GIS tools.

### Important

**Raw GIS data does not automatically belong in Git.**

Some GIS sources can be large.

We'll decide what gets versioned after inspecting the dataset.

---

# TASK 2.2 — Create a Python Virtual Environment

Open PowerShell.

Run:

```powershell
cd C:\Dev\Project543
```

Then:

```powershell
python -m venv .venv
```

This creates:

```text
Project543/
└── .venv/
```

The virtual environment isolates Project 543's Python tools from the rest of your computer.

Activate it:

```powershell
.\.venv\Scripts\Activate.ps1
```

You should see something like:

```text
(.venv) PS C:\Dev\Project543>
```

If Windows PowerShell refuses to run the activation script, **stop there and send me the error**. Don't change execution policies yourself.

---

# TASK 2.3 — Install the GIS Libraries

With `(.venv)` active, run:

```powershell
python -m pip install --upgrade pip
```

Then:

```powershell
pip install geopandas
```

GeoPandas gives Python the ability to work with geographic vector data.

Then verify:

```powershell
python -c "import geopandas as gpd; print(gpd.__version__)"
```

You should get a version number.

Also test Shapely:

```powershell
python -c "import shapely; print(shapely.__version__)"
```

### Definition of Done

Both commands return version numbers.

---

# TASK 2.4 — Acquire the Prototype Constituency Dataset

For this spike, use the 2024 543-seat GeoJSON from the open 2024 maps project.

[2024 Maps Supplement — India 543 constituency GeoJSON](https://github.com/shijithpk/2024_maps_supplement?utm_source=chatgpt.com)

The repository specifically contains:

```text
india_ls_seats_543.geojson
```

and documents that this version incorporates updated Assam and J&K seat boundaries into a 543-feature map. ([GitHub][3])

### Download

Open the repository.

Locate:

```text
india_ls_seats_543.geojson
```

Download the **raw file**.

Place it here:

```text
C:\Dev\Project543\tools\gis\source\
```

So you should have:

```text
tools/
└── gis/
    └── source/
        └── india_ls_seats_543.geojson
```

### Do not rename it yet.

We want to preserve the source filename.

---

# TASK 2.5 — Inspect the GeoJSON Without Editing It

Open the file in VS Code.

Do not manually edit anything.

You should see a structure broadly resembling:

```json
{
    "type": "FeatureCollection",
    "features": [
        {
            "type": "Feature",
            "properties": {
                ...
            },
            "geometry": {
                ...
            }
        }
    ]
}
```

The important things are:

### FeatureCollection

The entire dataset.

### Features

Individual constituency geometries.

### Properties

Metadata such as constituency number/name/code.

### Geometry

The polygon or multipolygon defining the constituency.

---

# TASK 2.6 — Write the First GIS Inspection Script

Create:

```text
tools\gis\scripts\inspect_geojson.py
```

Put this code inside:

```python
from pathlib import Path
import geopandas as gpd


PROJECT_ROOT = Path(__file__).resolve().parents[3]

SOURCE_FILE = (
    PROJECT_ROOT
    / "tools"
    / "gis"
    / "source"
    / "india_ls_seats_543.geojson"
)


def main() -> None:
    print("Project 543 GIS Inspection")
    print("==========================")
    print(f"Source: {SOURCE_FILE}")
    print()

    if not SOURCE_FILE.exists():
        raise FileNotFoundError(
            f"GIS source file not found: {SOURCE_FILE}"
        )

    gdf = gpd.read_file(SOURCE_FILE)

    print(f"Feature count: {len(gdf)}")
    print(f"CRS: {gdf.crs}")
    print()
    print("Columns:")
    print(list(gdf.columns))
    print()
    print("First feature:")
    print(gdf.iloc[0])


if __name__ == "__main__":
    main()
```

---

# TASK 2.7 — Run the Inspection

With the virtual environment active:

```powershell
python tools\gis\scripts\inspect_geojson.py
```

You should get information about:

* Feature count
* Coordinate reference system
* Columns
* First feature

### Critical point

We are expecting:

```text
Feature count: 543
```

If it says:

```text
543
```

excellent.

If it says anything else:

**STOP.**

Do not attempt to fix the dataset manually.

Send me the output.

---

# TASK 2.8 — Create a Proper GIS Validator

Once the inspection succeeds, create:

```text
tools\gis\scripts\validate_geojson.py
```

Use:

```python
from pathlib import Path
import geopandas as gpd


EXPECTED_FEATURE_COUNT = 543

PROJECT_ROOT = Path(__file__).resolve().parents[3]

SOURCE_FILE = (
    PROJECT_ROOT
    / "tools"
    / "gis"
    / "source"
    / "india_ls_seats_543.geojson"
)


def main() -> None:
    print("Project 543 GIS Validation")
    print("==========================")

    if not SOURCE_FILE.exists():
        raise FileNotFoundError(
            f"Missing source dataset: {SOURCE_FILE}"
        )

    gdf = gpd.read_file(SOURCE_FILE)

    feature_count = len(gdf)

    print(f"Feature count: {feature_count}")

    if feature_count != EXPECTED_FEATURE_COUNT:
        raise ValueError(
            f"Expected {EXPECTED_FEATURE_COUNT} features, "
            f"found {feature_count}."
        )

    print("Feature count: PASS")

    if gdf.crs is None:
        raise ValueError("Dataset has no CRS.")

    print(f"CRS: {gdf.crs}")
    print("CRS: PASS")

    invalid_geometry_count = (~gdf.geometry.is_valid).sum()

    print(
        f"Invalid geometries: "
        f"{invalid_geometry_count}"
    )

    if invalid_geometry_count != 0:
        raise ValueError(
            f"Found {invalid_geometry_count} "
            "invalid geometries."
        )

    print("Geometry validity: PASS")

    empty_geometry_count = gdf.geometry.is_empty.sum()

    print(
        f"Empty geometries: "
        f"{empty_geometry_count}"
    )

    if empty_geometry_count != 0:
        raise ValueError(
            f"Found {empty_geometry_count} "
            "empty geometries."
        )

    print("Geometry emptiness: PASS")

    print()
    print("GIS VALIDATION PASSED")


if __name__ == "__main__":
    main()
```

Run:

```powershell
python tools\gis\scripts\validate_geojson.py
```

---

# TASK 2.9 — Do NOT Assume the Data Is Perfect

This is an important professional distinction.

A dataset can have:

```text
543 features
```

and still be wrong.

We eventually need to validate:

* unique constituency IDs
* names
* state associations
* geometry
* CRS
* duplicate features
* missing features
* unexpected features
* geometry type
* coordinate ranges

For Sprint 2, we're establishing the first validation layer.

We'll expand this validator later.

---

# TASK 2.10 — Inspect the Actual Properties

The inspection script will tell us the column names.

Suppose it gives something like:

```text
PC_NO
PC_NAME
STATE
...
```

**Do not assume those names are exactly correct.**

Your output determines the next step.

We need to know the actual schema before writing the conversion pipeline.

This is why the inspection happens before the renderer.

---

# TASK 2.11 — Create a Processed Data Directory

After validation, we'll eventually generate:

```text
tools/
└── gis/
    └── processed/
        └── ...
```

But **don't create a processed dataset yet**.

First we need to understand:

* CRS
* coordinate structure
* property schema
* geometry types
* polygon complexity

Only then do we decide how to convert it.

---

# TASK 2.12 — Git Checkpoint

At this point, assuming the validation passes, run:

```powershell
git status
```

We should see our new:

```text
tools/
```

files.

Before committing, we'll decide whether the raw GeoJSON should be tracked.

### My current recommendation

Don't commit the raw source yet if it is large.

Instead, create a source manifest documenting:

```text
Dataset:
india_ls_seats_543.geojson

Purpose:
Project 543 GIS technical spike

Source:
2024 Maps Supplement

Feature count:
543

Acquisition date:
2026-08-25

Status:
Prototype / visualization dataset

Production status:
NOT YET APPROVED
```

We'll formalize this after we inspect the file.

---

# TASK 2.13 — First Godot GIS Test

We are **not yet going to implement a full GeoJSON parser in GDScript**.

That's an architectural trap.

Instead, our Python pipeline will eventually convert:

```text
GeoJSON
```

into:

```text
Project 543 runtime map data
```

that Godot can consume efficiently.

For the first Godot test, we only need a **single processed polygon**.

Our eventual runtime representation might look conceptually like:

```json
{
    "id": 12,
    "name": "Vijayawada",
    "state": "Andhra Pradesh",
    "geometry": [
        [x1, y1],
        [x2, y2],
        [x3, y3]
    ]
}
```

But we aren't defining the final schema yet.

**The GIS data inspection must come first.**

---

# TASK 2.14 — Map Rendering Strategy

Once we know the actual geometry structure, we'll implement a renderer using Godot's 2D polygon functionality.

The conceptual architecture will be:

```text
MapController
      │
      ├── ConstituencyRenderer
      │          │
      │          └── Polygon
      │
      └── ConstituencySelection
                    │
                    ↓
              Selected ID
                    │
                    ↓
             Info Panel
```

Eventually:

```text
543 data records
      ↓
543 rendered constituency objects
      ↓
mouse selection
      ↓
constituency ID
```

But we won't write that until our processed data is correct.

---

# TASK 2.15 — The First Real Technical Test

Our eventual test scene should show:

```text
┌───────────────────────────────────────────────┐
│ PROJECT 543                                   │
│                                               │
│                  INDIA                        │
│                                               │
│       [543 constituency polygons]             │
│                                               │
│                                               │
│                                               │
├───────────────────────────────────────────────┤
│ Selected: Vijayawada                          │
│ State: Andhra Pradesh                         │
│ Constituency ID: 12                           │
└───────────────────────────────────────────────┘
```

Clicking another polygon should change:

```text
Selected:
State:
Constituency ID:
```

No political simulation yet.

No party colours.

No support.

No elections.

Just:

> **Can the map understand what constituency I clicked?**

---

# TASK 2.16 — Performance Test

Once all 543 polygons render, we'll measure:

* startup time
* frame rate
* memory
* selection latency
* polygon complexity

We don't optimize prematurely.

But we will record the baseline.

If 543 polygons run perfectly, excellent.

If they don't, we'll fix the architecture before proceeding.

---

# Sprint 2 Milestones

We're going to treat Sprint 2 as several gates:

### Gate A — Data Acquisition

```text
543-seat GeoJSON acquired
```

### Gate B — Data Validation

```text
543 features
valid geometry
known CRS
known properties
```

### Gate C — Processing

```text
GeoJSON
↓
Project 543 runtime format
```

### Gate D — Rendering

```text
Runtime data
↓
Godot polygons
```

### Gate E — Interaction

```text
Mouse click
↓
Constituency identification
```

### Gate F — Full Map

```text
543 polygons
↓
543 selectable constituencies
```

Only after **Gate F** will Sprint 2 be considered complete.

---

# Important: don't jump ahead

Right now, I want you to execute only through **Task 2.8** first.

Specifically:

1. Create `tools/gis/source`
2. Create `tools/gis/processed`
3. Create `tools/gis/scripts`
4. Create the Python virtual environment
5. Install GeoPandas
6. Download `india_ls_seats_543.geojson`
7. Run `inspect_geojson.py`
8. Run `validate_geojson.py`

Then stop.

### Send me the complete output of:

```powershell
python tools\gis\scripts\inspect_geojson.py
```

and:

```powershell
python tools\gis\scripts\validate_geojson.py
```

**Do not continue to the Godot rendering stage yet.**

The exact schema returned by the dataset will determine how I design our conversion pipeline. That is the professional approach: **inspect the real data first, then write the importer around the data rather than guessing its structure.**

The 543-seat source we selected is specifically documented as a 543-feature 2024-oriented map, while the ECI's own 2024 material confirms the 543-seat framework and the special Assam/J&K boundary treatment, so we're starting this spike from a defensible baseline rather than an old generic India constituency map. ([GitHub][3])

[1]: https://projects.datameet.org/maps/parliamentary-constituencies/?utm_source=chatgpt.com "Parliamentary constituencies - Community Created Maps of India"
[2]: https://yashveeeeeeer.github.io/india-geodata/ "Download India’s Open Geospatial Data | India Geodata"
[3]: https://github.com/shijithpk/2024_maps_supplement?utm_source=chatgpt.com "GitHub - shijithpk/2024_maps_supplement: Supplementing Datameet maps with some newer, updated ones for the 2024 elections · GitHub"


AFTER VALIDATION++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++













Excellent. **Sprint 2 has reached the critical GIS-validation gate successfully.** The corrected 543-seat dataset is now our validated source of truth.

One note: I can keep the project state in this conversation, but I can’t directly update persistent memory because Memory is disabled. If you want persistent memory, enable it under **Settings → Personalization → Memory**.

# PROJECT 543 — SPRINT 2

## GIS Foundation & Constituency Data Pipeline

**Platform:** Windows 11
**Engine:** Godot
**Language/tooling:** Python + Godot
**Status:** In progress
**Current validated dataset:** `india_ls_seats_543_fixed.geojson`

### Sprint 2 objective

By the end of Sprint 2, we want:

```text
Raw GIS data
    ↓
fix_geojson.py
    ↓
india_ls_seats_543_fixed.geojson
    ↓
validation script
    ↓
VALIDATED
    ↓
Godot imports dataset
    ↓
543 constituencies loaded
    ↓
543 polygons rendered
    ↓
Every constituency clickable
    ↓
Click → constituency information
```

The first five steps are now working.

**The remaining Sprint 2 work is the actual Godot GIS technical proof.**

---

# SPRINT 2 — TASK BREAKDOWN

We will finish Sprint 2 in these tasks:

| Task | Objective                          | Status |
| ---- | ---------------------------------- | ------ |
| 2.1  | GIS source acquisition             | ✅      |
| 2.2  | GIS cleaning/fixing                | ✅      |
| 2.3  | GIS validation                     | ✅      |
| 2.4  | Establish game-data pipeline       | ⬅️ NOW |
| 2.5  | Create Godot GIS loader            |        |
| 2.6  | Parse all 543 features             |        |
| 2.7  | Render constituency polygons       |        |
| 2.8  | Make polygons clickable            |        |
| 2.9  | Display constituency metadata      |        |
| 2.10 | Handle map interaction states      |        |
| 2.11 | Performance/basic robustness check |        |
| 2.12 | Sprint 2 automated validation      |        |
| 2.13 | Git commit + Sprint 2 completion   |        |

We will **not** jump ahead to personas, elections, AI, economy, etc.

---

# TASK 2.4 — ESTABLISH THE GAME DATA PIPELINE

This is important.

We currently have:

```text
scripts/
    fix_geojson.py

GIS source/
    india_ls_seats_543_fixed.geojson
```

We need to establish a clean separation between:

**source data**

and

**runtime game data**.

Do not let Godot depend on some random file sitting in a Downloads folder.

---

## 2.4.1 Create the data directories

Inside your Project 543 project directory, create:

```text
data/
    raw/
    validated/
    game/
```

So your project should approximately look like:

```text
Project543/
│
├── .git/
├── .gitignore
│
├── project.godot
│
├── scripts/
│   ├── fix_geojson.py
│   └── validate_geojson.py
│
├── data/
│   ├── raw/
│   ├── validated/
│   └── game/
│
└── ...
```

### What these folders mean

`raw/`

Original external GIS data.

We don't modify this.

`validated/`

Data that has passed our validation/cleaning pipeline.

`game/`

Data that the actual Godot game consumes.

This separation will become extremely valuable later.

---

# TASK 2.4.2 — Put the corrected GIS file in `validated`

Take:

```text
india_ls_seats_543_fixed.geojson
```

and place it here:

```text
data/validated/india_ls_seats_543.geojson
```

I deliberately recommend renaming it.

The fact that it was "fixed" is a **pipeline implementation detail**, not something the game should care about.

Therefore:

```text
india_ls_seats_543_fixed.geojson
```

becomes:

```text
data/validated/india_ls_seats_543.geojson
```

Your pipeline is:

```text
raw
 ↓
fix_geojson.py
 ↓
validated
 ↓
validation
 ↓
Godot
```

---

# TASK 2.4.3 — Keep the original source

Do **not** delete your original GIS file.

We want:

```text
data/
├── raw/
│   └── [original source]
│
├── validated/
│   └── india_ls_seats_543.geojson
│
└── game/
```

The raw file is our archaeological record.

If something goes wrong six months from now, we can reconstruct the pipeline.

---

# TASK 2.4.4 — Verify the validated file

Open PowerShell in the Project 543 root.

Run:

```powershell
python scripts\validate_geojson.py data\validated\india_ls_seats_543.geojson
```

Your expected result is equivalent to:

```text
Feature count: 543
Feature count: PASS
CRS: EPSG:4326
CRS: PASS
Invalid geometries: 0
Geometry validity: PASS
Empty geometries: 0
Geometry emptiness: PASS

GIS VALIDATION PASSED
```

If your validator uses a different command-line format, **do not modify it just to match my example**. Use the invocation that already worked for you.

---

# TASK 2.5 — IMPORTANT: INSPECT THE ACTUAL DATA SCHEMA

Before we write a single line of Godot map-rendering code, we need to know exactly what information exists inside each GeoJSON feature.

This is a professional requirement.

The geometry tells us **where the constituency is**.

The properties tell us **what the constituency is**.

We need both.

---

## 2.5.1 Create an inspection script

Create:

```text
scripts/inspect_geojson.py
```

Put this inside:

```python
import json
import sys


def main():
    if len(sys.argv) != 2:
        print("Usage: python scripts/inspect_geojson.py <geojson>")
        sys.exit(1)

    path = sys.argv[1]

    with open(path, "r", encoding="utf-8") as f:
        data = json.load(f)

    features = data.get("features", [])

    print(f"Feature count: {len(features)}")
    print()

    if not features:
        print("No features found.")
        sys.exit(1)

    print("First feature:")
    print(json.dumps(features[0], indent=2, ensure_ascii=False))

    print()
    print("Property keys:")
    
    properties = features[0].get("properties", {})

    for key, value in properties.items():
        print(f"  {key}: {value!r}")


if __name__ == "__main__":
    main()
```

---

# TASK 2.5.2 — Run it

Run:

```powershell
python scripts\inspect_geojson.py data\validated\india_ls_seats_543.geojson
```

You will get something like:

```text
Feature count: 543

First feature:
{
    ...
}

Property keys:
    ...
```

**Do not make assumptions about the property names.**

For example, I don't want us blindly assuming the data contains:

```text
name
state
id
population
```

It might instead contain:

```text
PC_NO
PC_NAME
ST_NAME
```

or something completely different.

---

# STOP POINT

After running the inspection script, send me the complete output, or at minimum:

```text
Feature count:
Property keys:
```

and the first feature's `properties` object.

**Do not proceed to writing the Godot loader yet.**

This is deliberate.

The next implementation depends on the actual schema of your corrected GIS file.

---

# WHY WE ARE DOING THIS

A common beginner approach would be:

> "Let's make a Godot script that loads GeoJSON."

That's too early.

Our professional pipeline is:

```text
VERIFY SOURCE
     ↓
VERIFY SCHEMA
     ↓
DEFINE GAME DATA CONTRACT
     ↓
BUILD IMPORTER
     ↓
VALIDATE IMPORTED DATA
     ↓
RENDER
     ↓
INTERACT
```

The GIS file is an external data format.

Godot should eventually receive a **clean, predictable constituency data model**, rather than having gameplay code understand arbitrary GeoJSON properties.

That distinction will save us considerable trouble later.

---

# THE ARCHITECTURE WE ARE BUILDING

Ultimately, I want this:

```text
                 GIS SOURCE
                     │
                     ▼
              fix_geojson.py
                     │
                     ▼
             VALIDATED GEOJSON
                     │
                     ▼
          validate_geojson.py
                     │
                     ▼
              DATA CONTRACT
                     │
                     ▼
          ┌─────────────────────┐
          │ ConstituencyData    │
          │                     │
          │ ID                  │
          │ Name                │
          │ State               │
          │ Geometry            │
          │ Population          │
          │ Turnout             │
          │ Persona Distribution│
          │ etc.                │
          └──────────┬──────────┘
                     │
                     ▼
             GODOT DATA LAYER
                     │
                     ▼
             MAP RENDERER
                     │
                     ▼
            INTERACTIVE MAP
```

Eventually, the same `ConstituencyData` structure will be consumed by:

```text
Map Renderer
     │
     ├── Constituency UI
     │
     ├── Campaign System
     │
     ├── AI
     │
     ├── Election Engine
     │
     └── Save System
```

That's why we are being careful now.

---

# SPRINT 2 SUCCESS CRITERIA

We are **not** declaring Sprint 2 complete merely because the GeoJSON validates.

The actual definition of done is:

### GIS

* [x] 543 features
* [x] EPSG:4326
* [x] 0 invalid geometries
* [x] 0 empty geometries
* [ ] Schema documented
* [ ] Validated dataset placed in project
* [ ] Data pipeline established

### Godot technical proof

* [ ] Godot loads the dataset
* [ ] 543 constituencies are instantiated
* [ ] 543 polygons render
* [ ] Map can be viewed correctly
* [ ] Constituencies are individually identifiable
* [ ] Clicking a constituency selects only that constituency
* [ ] Selected constituency is visually highlighted
* [ ] Constituency information appears
* [ ] No crashes from normal map interaction

### Verification

* [ ] Automated 543-feature load check
* [ ] No duplicate constituency IDs
* [ ] No missing required metadata
* [ ] Basic performance check
* [ ] Git commit
* [ ] Sprint 2 marked complete

---

# ONE MORE IMPORTANT DESIGN DECISION

For the first map prototype, **we will not attempt to make a beautiful India map**.

The first successful build can look ugly.

It can be:

```text
┌──────────────────────────────────┐
│          PROJECT 543             │
│                                  │
│       [543 coloured shapes]      │
│                                  │
│                                  │
│  Selected: Constituency 123      │
│  State: XXXXX                    │
└──────────────────────────────────┘
```

If we can prove:

> **543 real GIS polygons → Godot → individually clickable**

then we have eliminated one of the largest technical risks in Project 543.

Only **after that** do we make it beautiful.

---

## Your immediate instruction

Do **only this now**:

1. Create `scripts/inspect_geojson.py` using the code above.
2. Run:

```powershell
python scripts\inspect_geojson.py data\validated\india_ls_seats_543.geojson
```

3. Send me the output.

**Do not start creating the Godot map yet.**

Once I see the actual schema, I will define our **Project 543 Constituency Data Contract**, then we will implement the importer and proceed through the remaining Sprint 2 tasks one verified gate at a time.

















INSPECTION OVER++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++























Excellent. **This is exactly the validation evidence we needed.** The dataset is structurally healthy: **543 features, EPSG:4326, zero invalid geometries, zero empty geometries**, and the property schema is present.

We can now move forward with **Sprint 2**. I’ll treat the corrected `india_ls_seats_543_fixed.geojson` as the authoritative Sprint-2 source and keep the original raw source untouched.

One important limitation: I can’t actually update persistent memory from this chat. If you want project memory retained across chats, enable it under **Settings → Personalization → Memory**. For this conversation, I’ll use everything established here as the project baseline.

# PROJECT 543 — SPRINT 2

## GIS Data Integration & Runtime Map Foundation

### Sprint 2 objective

By the end of Sprint 2, we want:

> **Godot can load the validated 543-seat GeoJSON dataset at runtime, convert its geographic coordinates into a usable game/world coordinate system, create 543 seat records, and render a basic India constituency map foundation.**

We are **not** building gameplay yet.

We are establishing the data → runtime → visualization pipeline properly.

---

# Sprint 2 Definition of Done

We will consider Sprint 2 complete only when all of these pass:

### Data

* [x] 543 GeoJSON features
* [x] EPSG:4326
* [x] No invalid geometries
* [x] No empty geometries
* [x] Required properties exist
* [x] `unique_id` exists
* [ ] Duplicate `unique_id` check passes
* [ ] Missing/null property check passes
* [ ] Seat-code consistency check passes
* [ ] Runtime-safe dataset generated

### Python pipeline

* [x] `fix_geojson.py`
* [x] GIS validation script
* [ ] Runtime preparation script
* [ ] Deterministic output
* [ ] Validation report

### Godot

* [ ] Dataset copied into project assets
* [ ] GeoJSON loaded by Godot
* [ ] JSON parsed successfully
* [ ] 543 features loaded
* [ ] Seat records created
* [ ] Coordinates transformed
* [ ] Map bounds calculated
* [ ] Basic map rendered
* [ ] No runtime errors

### Engineering

* [ ] Clear directory structure
* [ ] No generated files mixed with source data
* [ ] No hard-coded 543-seat geometry in GDScript
* [ ] Git commit
* [ ] Sprint 2 validation evidence

---

# PART 1 — Freeze the validated source

Do **not** modify the corrected file anymore unless another validation problem is discovered.

Your authoritative source is:

```text
india_ls_seats_543_fixed.geojson
```

We want a clean separation:

```text
project543/
│
├── data/
│   ├── source/
│   │   └── india_ls_seats_543_fixed.geojson
│   │
│   └── generated/
│
├── scripts/
│   ├── fix_geojson.py
│   ├── validate_geojson.py
│   └── prepare_geojson.py
│
├── godot/
│   └── ...
│
└── README.md
```

If your existing structure differs, **don't reorganize everything blindly**. We can adapt to what you already have.

---

# PART 2 — Add the second-level data validation

Your current validation proves geometry health.

Now we need to prove **semantic data health**.

Create:

```text
scripts/validate_seat_data.py
```

Put this in it:

```python
import json
import sys
from pathlib import Path


REQUIRED_PROPERTIES = [
    "state_ut_name",
    "ls_seat_name",
    "state_ut_code",
    "ls_seat_code",
    "unique_id",
]


def fail(message):
    print(f"FAIL: {message}")
    sys.exit(1)


def main():
    if len(sys.argv) != 2:
        print("Usage: python validate_seat_data.py <geojson>")
        sys.exit(1)

    path = Path(sys.argv[1])

    if not path.exists():
        fail(f"File not found: {path}")

    try:
        with path.open("r", encoding="utf-8") as f:
            data = json.load(f)
    except Exception as exc:
        fail(f"Could not parse JSON: {exc}")

    if data.get("type") != "FeatureCollection":
        fail("Root object is not a FeatureCollection")

    features = data.get("features")

    if not isinstance(features, list):
        fail("features is not a list")

    print(f"Feature count: {len(features)}")

    if len(features) != 543:
        fail(f"Expected 543 features, got {len(features)}")

    unique_ids = set()

    for index, feature in enumerate(features):
        properties = feature.get("properties")

        if not isinstance(properties, dict):
            fail(f"Feature {index}: properties missing or invalid")

        for key in REQUIRED_PROPERTIES:
            if key not in properties:
                fail(f"Feature {index}: missing property '{key}'")

            value = properties[key]

            if value is None or str(value).strip() == "":
                fail(f"Feature {index}: empty property '{key}'")

        unique_id = str(properties["unique_id"]).strip()

        if unique_id in unique_ids:
            fail(f"Duplicate unique_id: {unique_id}")

        unique_ids.add(unique_id)

    print("Required properties: PASS")
    print("unique_id uniqueness: PASS")
    print(f"Unique IDs: {len(unique_ids)}")
    print("SEAT DATA VALIDATION PASSED")


if __name__ == "__main__":
    main()
```

---

# PART 3 — Run the semantic validation

From the project root:

```powershell
python scripts\validate_seat_data.py data\source\india_ls_seats_543_fixed.geojson
```

Expected:

```text
Feature count: 543
Required properties: PASS
unique_id uniqueness: PASS
Unique IDs: 543
SEAT DATA VALIDATION PASSED
```

If it fails, **stop there**.

Don't patch the dataset manually yet.

Give me the exact failure output.

---

# PART 4 — Create the runtime data pipeline

This is an important architectural decision.

We don't want Godot doing unnecessary GIS preprocessing.

Python is our **data preparation layer**.

Godot is our **runtime layer**.

So:

```text
RAW/VERIFIED GEOJSON
        ↓
Python validation/preparation
        ↓
runtime GeoJSON
        ↓
Godot
        ↓
Seat objects
        ↓
Map rendering
```

That's a clean architecture.

---

# PART 5 — Create `prepare_geojson.py`

Create:

```text
scripts/prepare_geojson.py
```

Use:

```python
import json
import sys
from pathlib import Path


REQUIRED_PROPERTIES = [
    "state_ut_name",
    "ls_seat_name",
    "state_ut_code",
    "ls_seat_code",
    "unique_id",
]


def fail(message):
    print(f"FAIL: {message}")
    sys.exit(1)


def main():
    if len(sys.argv) != 3:
        print(
            "Usage: python prepare_geojson.py "
            "<input.geojson> <output.geojson>"
        )
        sys.exit(1)

    input_path = Path(sys.argv[1])
    output_path = Path(sys.argv[2])

    if not input_path.exists():
        fail(f"Input file not found: {input_path}")

    with input_path.open("r", encoding="utf-8") as f:
        data = json.load(f)

    if data.get("type") != "FeatureCollection":
        fail("Input is not a FeatureCollection")

    features = data.get("features", [])

    if len(features) != 543:
        fail(f"Expected 543 features, got {len(features)}")

    prepared_features = []

    seen_ids = set()

    for index, feature in enumerate(features):
        properties = feature.get("properties", {})
        geometry = feature.get("geometry")

        for key in REQUIRED_PROPERTIES:
            if key not in properties:
                fail(f"Feature {index}: missing '{key}'")

        unique_id = str(properties["unique_id"]).strip()

        if unique_id in seen_ids:
            fail(f"Duplicate unique_id: {unique_id}")

        seen_ids.add(unique_id)

        prepared_features.append(
            {
                "type": "Feature",
                "properties": {
                    "state_ut_name": str(
                        properties["state_ut_name"]
                    ).strip(),

                    "ls_seat_name": str(
                        properties["ls_seat_name"]
                    ).strip(),

                    "state_ut_code": str(
                        properties["state_ut_code"]
                    ).strip(),

                    "ls_seat_code": str(
                        properties["ls_seat_code"]
                    ).strip(),

                    "unique_id": unique_id,
                },
                "geometry": geometry,
            }
        )

    output = {
        "type": "FeatureCollection",
        "features": prepared_features,
    }

    output_path.parent.mkdir(parents=True, exist_ok=True)

    with output_path.open("w", encoding="utf-8") as f:
        json.dump(
            output,
            f,
            ensure_ascii=False,
            separators=(",", ":"),
        )

    print(f"Prepared features: {len(prepared_features)}")
    print(f"Output: {output_path}")
    print("RUNTIME GEOJSON PREPARATION PASSED")


if __name__ == "__main__":
    main()
```

---

# PART 6 — Generate the runtime dataset

Run:

```powershell
python scripts\prepare_geojson.py data\source\india_ls_seats_543_fixed.geojson data\generated\india_ls_seats_543_runtime.geojson
```

Expected:

```text
Prepared features: 543
Output: data\generated\india_ls_seats_543_runtime.geojson
RUNTIME GEOJSON PREPARATION PASSED
```

Now we have:

```text
data/
├── source/
│   └── india_ls_seats_543_fixed.geojson
│
└── generated/
    └── india_ls_seats_543_runtime.geojson
```

---

# PART 7 — Put runtime data inside Godot

Now identify your Godot project directory.

Inside it create:

```text
data/
```

Then:

```text
data/
└── india_ls_seats_543_runtime.geojson
```

So the Godot project becomes approximately:

```text
godot_project/
│
├── project.godot
├── scenes/
├── scripts/
├── data/
│   └── india_ls_seats_543_runtime.geojson
└── ...
```

**Important:**

Do not put the entire raw GIS development directory inside the Godot project.

Godot only needs the runtime asset.

---

# PART 8 — Create the GIS runtime loader

Create:

```text
scripts/gis_data_loader.gd
```

Use:

```gdscript
class_name GISDataLoader
extends RefCounted


func load_seats(path: String) -> Array:
	var file := FileAccess.open(path, FileAccess.READ)

	if file == null:
		push_error("Could not open GIS data: " + path)
		return []

	var text := file.get_as_text()
	var json := JSON.new()

	var parse_result := json.parse(text)

	if parse_result != OK:
		push_error(
			"Could not parse GIS JSON: "
			+ json.get_error_message()
		)
		return []

	var data = json.data

	if typeof(data) != TYPE_DICTIONARY:
		push_error("GIS root is not a dictionary")
		return []

	if data.get("type", "") != "FeatureCollection":
		push_error("GIS root is not a FeatureCollection")
		return []

	var features = data.get("features", [])

	if typeof(features) != TYPE_ARRAY:
		push_error("GIS features is not an array")
		return []

	var seats: Array = []

	for feature in features:
		var properties: Dictionary = feature.get(
			"properties",
			{}
		)

		var geometry: Dictionary = feature.get(
			"geometry",
			{}
		)

		var seat := {
			"state_ut_name": properties.get(
				"state_ut_name",
				""
			),

			"ls_seat_name": properties.get(
				"ls_seat_name",
				""
			),

			"state_ut_code": properties.get(
				"state_ut_code",
				""
			),

			"ls_seat_code": properties.get(
				"ls_seat_code",
				""
			),

			"unique_id": properties.get(
				"unique_id",
				""
			),

			"geometry": geometry
		}

		seats.append(seat)

	return seats
```

---

# PART 9 — Create the geographic coordinate converter

This is where we establish the first real GIS/game boundary.

GeoJSON uses:

```text
longitude, latitude
```

Godot doesn't understand geographic coordinates as a useful game-world coordinate system.

For the first prototype, we'll use a simple **equirectangular projection**.

Create:

```text
scripts/geo_projection.gd
```

```gdscript
class_name GeoProjection
extends RefCounted


var min_lon: float
var max_lon: float
var min_lat: float
var max_lat: float

var width: float
var height: float


func setup(
	min_longitude: float,
	max_longitude: float,
	min_latitude: float,
	max_latitude: float,
	map_width: float,
	map_height: float
) -> void:
	min_lon = min_longitude
	max_lon = max_longitude

	min_lat = min_latitude
	max_lat = max_latitude

	width = map_width
	height = map_height


func project(longitude: float, latitude: float) -> Vector2:
	var x := inverse_lerp(
		min_lon,
		max_lon,
		longitude
	)

	var y := inverse_lerp(
		max_lat,
		min_lat,
		latitude
	)

	return Vector2(
		x * width,
		y * height
	)
```

Notice the latitude inversion.

Geographic latitude increases northward.

Screen Y increases downward.

So:

```text
north → smaller screen Y
south → larger screen Y
```

---

# PART 10 — We need dataset bounds

Don't hard-code India's bounds yet.

The runtime loader should calculate them from the dataset.

Create:

```text
scripts/gis_bounds.gd
```

```gdscript
class_name GISBounds
extends RefCounted


var min_lon := INF
var max_lon := -INF
var min_lat := INF
var max_lat := -INF


func include_coordinate(
	longitude: float,
	latitude: float
) -> void:
	min_lon = min(min_lon, longitude)
	max_lon = max(max_lon, longitude)

	min_lat = min(min_lat, latitude)
	max_lat = max(max_lat, latitude)


func is_valid() -> bool:
	return (
		min_lon != INF
		and max_lon != -INF
		and min_lat != INF
		and max_lat != -INF
	)


func get_center() -> Vector2:
	return Vector2(
		(min_lon + max_lon) * 0.5,
		(min_lat + max_lat) * 0.5
	)
```

---

# PART 11 — Geometry extraction

Your GeoJSON contains:

```text
MultiPolygon
```

So the renderer needs to understand:

```text
Feature
 └── geometry
      └── coordinates
           └── polygon
                └── ring
                     └── [longitude, latitude]
```

For now, **do not attempt holes, topology repair, triangulation, or advanced GIS rendering.**

First we establish the pipeline.

Create:

```text
scripts/geo_geometry.gd
```

```gdscript
class_name GeoGeometry
extends RefCounted


static func extract_points(
	geometry: Dictionary
) -> Array:
	var geometry_type := geometry.get(
		"type",
		""
	)

	var coordinates = geometry.get(
		"coordinates",
		[]
	)

	var rings: Array = []

	if geometry_type == "Polygon":
		for ring in coordinates:
			rings.append(ring)

	elif geometry_type == "MultiPolygon":
		for polygon in coordinates:
			for ring in polygon:
				rings.append(ring)

	return rings
```

This gives us all rings.

---

# PART 12 — Create the first map renderer

Create:

```text
scripts/india_map.gd
```

Start simple.

```gdscript
extends Node2D


const DATA_PATH := "res://data/india_ls_seats_543_runtime.geojson"

const MAP_WIDTH := 1200.0
const MAP_HEIGHT := 1000.0


var seats: Array = []
var projection: GeoProjection


func _ready() -> void:
	var loader := GISDataLoader.new()

	seats = loader.load_seats(DATA_PATH)

	print("GIS seats loaded: ", seats.size())

	if seats.size() != 543:
		push_error(
			"Expected 543 seats, got "
			+ str(seats.size())
		)
		return

	_build_projection()
	queue_redraw()


func _build_projection() -> void:
	var bounds := GISBounds.new()

	for seat in seats:
		var geometry: Dictionary = seat["geometry"]

		var rings := GeoGeometry.extract_points(
			geometry
		)

		for ring in rings:
			for coordinate in ring:
				if coordinate.size() < 2:
					continue

				var longitude := float(
					coordinate[0]
				)

				var latitude := float(
					coordinate[1]
				)

				bounds.include_coordinate(
					longitude,
					latitude
				)

	if not bounds.is_valid():
		push_error("Could not calculate GIS bounds")
		return

	print(
		"Bounds: ",
		bounds.min_lon,
		", ",
		bounds.min_lat,
		" -> ",
		bounds.max_lon,
		", ",
		bounds.max_lat
	)

	projection = GeoProjection.new()

	projection.setup(
		bounds.min_lon,
		bounds.max_lon,
		bounds.min_lat,
		bounds.max_lat,
		MAP_WIDTH,
		MAP_HEIGHT
	)
```

---

# PART 13 — Draw the constituency polygons

Add this to `india_map.gd`:

```gdscript
func _draw() -> void:
	if projection == null:
		return

	for seat in seats:
		var geometry: Dictionary = seat["geometry"]

		var rings := GeoGeometry.extract_points(
			geometry
		)

		for ring in rings:
			var points := PackedVector2Array()

			for coordinate in ring:
				if coordinate.size() < 2:
					continue

				var longitude := float(
					coordinate[0]
				)

				var latitude := float(
					coordinate[1]
				)

				points.append(
					projection.project(
						longitude,
						latitude
					)
				)

			if points.size() >= 2:
				draw_polyline(
					points,
					Color.WHITE,
					1.0,
					true
				)
```

This is deliberately primitive.

We're proving:

```text
GeoJSON
   ↓
JSON parser
   ↓
543 features
   ↓
MultiPolygon extraction
   ↓
longitude/latitude
   ↓
projection
   ↓
Godot drawing
```

That pipeline is the heart of Sprint 2.

---

# PART 14 — Create the map scene

In Godot:

Create a new scene:

```text
Node2D
```

Name it:

```text
IndiaMap
```

Attach:

```text
scripts/india_map.gd
```

Save:

```text
scenes/india_map.tscn
```

Then set it as the main scene.

---

# PART 15 — Run

Launch the game.

The first important console output should be:

```text
GIS seats loaded: 543
```

Then something similar to:

```text
Bounds: ...
```

And visually you should see a rough constituency map.

It will not look like the finished Project 543 map.

**That's expected.**

At this stage we're proving the data pipeline, not artistic quality.

---

# PART 16 — Important warning about the rendering approach

Our first renderer uses:

```gdscript
draw_polyline()
```

This is intentionally temporary.

It has limitations:

* polygon interiors aren't filled
* holes aren't handled
* multipolygon topology isn't fully represented
* antimeridian issues aren't handled
* projection isn't geographically accurate enough for final production
* thousands of vertices may eventually require optimization

We will improve this later.

**Do not optimize it prematurely.**

First establish correctness.

---

# PART 17 — Add a hard runtime assertion

Inside `_ready()`:

```gdscript
assert(
	seats.size() == 543,
	"Project 543 requires exactly 543 constituencies."
)
```

So:

```gdscript
func _ready() -> void:
	var loader := GISDataLoader.new()

	seats = loader.load_seats(DATA_PATH)

	print("GIS seats loaded: ", seats.size())

	assert(
		seats.size() == 543,
		"Project 543 requires exactly 543 constituencies."
	)

	_build_projection()
	queue_redraw()
```

This is important.

The number **543 is a domain invariant** for our current Project 543 dataset.

If the dataset unexpectedly changes, the application should tell us immediately.

---

# PART 18 — Add a duplicate-ID check inside Godot

Don't rely solely on Python.

Runtime validation should also protect us.

Add:

```gdscript
func _validate_seats() -> bool:
	var ids := {}

	for seat in seats:
		var unique_id: String = str(
			seat["unique_id"]
		)

		if unique_id.is_empty():
			push_error("Seat has empty unique_id")
			return false

		if ids.has(unique_id):
			push_error(
				"Duplicate unique_id: "
				+ unique_id
			)
			return false

		ids[unique_id] = true

	print(
		"Unique seat IDs validated: ",
		ids.size()
	)

	return ids.size() == 543
```

Then:

```gdscript
func _ready() -> void:
	var loader := GISDataLoader.new()

	seats = loader.load_seats(DATA_PATH)

	print("GIS seats loaded: ", seats.size())

	assert(
		seats.size() == 543,
		"Project 543 requires exactly 543 constituencies."
	)

	if not _validate_seats():
		return

	_build_projection()
	queue_redraw()
```

---

# PART 19 — Expected final runtime validation

We want Godot's output to look approximately like:

```text
GIS seats loaded: 543
Unique seat IDs validated: 543
Bounds: ...
```

No errors.

And the map should render.

---

# PART 20 — Test one known constituency

We already have a known first feature:

```text
state_ut_name: Haryana
ls_seat_name: Bhiwani-Mahendragarh
state_ut_code: S07
ls_seat_code: 8
unique_id: S07_8
```

Add temporarily:

```gdscript
print(
	"First seat: ",
	seats[0]["unique_id"],
	" | ",
	seats[0]["state_ut_name"],
	" | ",
	seats[0]["ls_seat_name"]
)
```

Expected:

```text
First seat: S07_8 | Haryana | Bhiwani-Mahendragarh
```

That gives us a full end-to-end identity test.

---

# PART 21 — Git checkpoint

Once the following work:

* semantic validation
* runtime preparation
* Godot data loading
* 543-seat assertion
* unique ID validation
* coordinate projection
* map rendering

all pass:

```powershell
git status
```

Review the files.

Then:

```powershell
git add .
```

Commit:

```powershell
git commit -m "Sprint 2: integrate validated 543-seat GIS dataset"
```

Then:

```powershell
git status
```

We want:

```text
nothing to commit, working tree clean
```

---

# Sprint 2 architecture after this work

We are deliberately establishing this architecture:

```text
                    PROJECT 543
                         │
                         ▼
              ┌─────────────────────┐
              │ Authoritative GIS   │
              │ Source              │
              │                     │
              │ 543 constituencies  │
              └──────────┬──────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │ Python GIS Pipeline │
              │                     │
              │ fix                 │
              │ validate             │
              │ prepare              │
              └──────────┬──────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │ Runtime GeoJSON     │
              │                     │
              │ 543 features        │
              └──────────┬──────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │ Godot Loader        │
              └──────────┬──────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │ Seat Records        │
              │                     │
              │ ID                  │
              │ State               │
              │ Constituency        │
              │ Geometry            │
              └──────────┬──────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │ Geo Projection      │
              └──────────┬──────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │ Map Renderer        │
              └─────────────────────┘
```

This is a **good foundation for the actual game**.

---

# Do NOT do these yet

For Sprint 2, resist the temptation to implement:

* elections
* political parties
* MPs
* voting
* campaigns
* population simulation
* AI
* economy
* save system
* animations
* sophisticated UI
* final map styling
* zoom/pan
* province/state gameplay
* procedural gameplay

Those belong later.

Right now we're making sure **the geographical foundation is trustworthy**.

---

# Your immediate execution order

Do these in exactly this order:

### 1.

Create:

```text
scripts/validate_seat_data.py
```

### 2.

Run:

```powershell
python scripts\validate_seat_data.py data\source\india_ls_seats_543_fixed.geojson
```

### 3.

Create:

```text
scripts/prepare_geojson.py
```

### 4.

Run:

```powershell
python scripts\prepare_geojson.py data\source\india_ls_seats_543_fixed.geojson data\generated\india_ls_seats_543_runtime.geojson
```

### 5.

Copy:

```text
india_ls_seats_543_runtime.geojson
```

into your Godot project's:

```text
data/
```

### 6.

Create:

```text
scripts/gis_data_loader.gd
scripts/geo_projection.gd
scripts/gis_bounds.gd
scripts/geo_geometry.gd
scripts/india_map.gd
```

### 7.

Create:

```text
scenes/india_map.tscn
```

### 8.

Run Godot.

### 9.

Verify:

```text
GIS seats loaded: 543
Unique seat IDs validated: 543
```

### 10.

Verify the map appears.

### 11.

Verify:

```text
First seat: S07_8 | Haryana | Bhiwani-Mahendragarh
```

### 12.

Commit:

```powershell
git add .
git commit -m "Sprint 2: integrate validated 543-seat GIS dataset"
```

---

## One thing I want you to do differently this Sprint

**Don't clean or manually alter the GeoJSON again.**

You've already done the difficult part: you produced a geometrically valid 543-feature source. From this point forward, we want transformations to be **reproducible through scripts**, not manual edits.

That gives Project 543 a proper professional data pipeline:

> **source → deterministic processing → validation → runtime asset → Godot**

Once you complete **Steps 1–4**, send me the exact terminal output. We will then verify the data layer before touching the Godot renderer.
